{
  "name": "@askua/ebcdic",
  "version": "1.0.0",
  "exports": "./mod.ts",
  "tasks": {
    "test": "deno test && deno test --doc mod.ts",
    "check": "deno fmt && deno lint && deno check . && deno task test && deno bench && deno publish --dry-run"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
